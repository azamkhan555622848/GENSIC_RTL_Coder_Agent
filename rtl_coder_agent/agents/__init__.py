"""RTL Coder Agent – agent role wrappers.

This sub-package groups together the different specialised agent
implementations (planner, generator, linter, simulator, debugger) plus the
orchestrator that stitches them together into a full workflow as described in
ImplementationPlan.md.
""" 